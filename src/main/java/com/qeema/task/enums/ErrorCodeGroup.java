package com.qeema.task.enums;

public enum ErrorCodeGroup {
    VALIDATION, // Errors related to data validation
    DATABASE,   // Errors interacting with the database
    NOT_FOUND,  // For when requested resources are not found
}